/**
 * anchors.mjs — hand-written "anchor" knowledge nodes for the recall-eval
 * fixture, each paired with a question (terse + chatty phrasing) whose
 * unique expected answer is that node's id.
 *
 * ORIGINAL content about a made-up software project ("Riverstone", a
 * task-orchestration / job-queue platform). Topical spread (decision /
 * convention / architecture / bug_pattern, terse+chatty question pairs,
 * abstain-style edge cases) mirrors the *style* of the Atlas eval's
 * questions.json but no content, wording, or node data is copied from it.
 *
 * Every anchor's `content` is written to be lexically distinctive enough
 * that hybrid (BM25 + semantic) retrieval should find it uniquely among
 * the filler/near-duplicate nodes generated by corpus.mjs.
 */

export const ANCHORS = [
    {
        id: 'fx-decision-lease-ttl',
        type: 'decision',
        label: 'Worker lease TTL fixed at 45 seconds',
        content: 'Riverstone worker leases expire after 45 seconds of no heartbeat. We chose 45s over the originally proposed 30s because p99 heartbeat latency under load spikes to 28s, which left almost no margin. A lease that expires early causes duplicate job execution, which is worse than a slightly slower failover.',
        tags: ['scheduler', 'lease', 'timeout'],
        terse: 'worker lease timeout value',
        chatty: 'Why did we land on 45 seconds for how long a worker can hold a job lease before it expires?',
    },
    {
        id: 'fx-decision-dlq-retention',
        type: 'decision',
        label: 'Dead-letter queue retains messages for 14 days',
        content: 'Messages that exhaust their retry budget move to the dead-letter queue and are retained for 14 days before automatic purge. 14 days was chosen to cover a full on-call rotation plus one buffer week, so nobody misses a poison message because they were out.',
        tags: ['dlq', 'retention', 'reliability'],
        terse: 'dead letter queue retention period',
        chatty: 'How long do we keep messages sitting in the dead-letter queue before they get purged automatically?',
    },
    {
        id: 'fx-decision-tenant-router-hash',
        type: 'decision',
        label: 'Tenant router uses consistent hashing, not round robin',
        content: 'The tenant router assigns each tenant to a shard using consistent hashing over tenant id rather than round robin. Round robin was tried first but caused a hot tenant to migrate shards on every scale event, invalidating its warm cache. Consistent hashing keeps a tenant pinned to the same shard across scale-ups.',
        tags: ['tenant-router', 'sharding', 'architecture'],
        terse: 'tenant router sharding strategy',
        chatty: 'What approach does the tenant router use to decide which shard a given tenant lands on?',
    },
    {
        id: 'fx-decision-config-loader-format',
        type: 'decision',
        label: 'Config loader standardized on TOML over YAML',
        content: 'Riverstone config files moved from YAML to TOML in the 2.0 rewrite. The driver was YAML\'s ambiguous type coercion (the classic Norway-problem "no" parsing as boolean false) silently corrupting a rate-limiter config in production. TOML has no such implicit coercion.',
        tags: ['config-loader', 'format', 'incident-driven'],
        terse: 'config file format choice',
        chatty: 'Why did the config loader switch away from YAML to TOML for its file format?',
    },
    {
        id: 'fx-convention-retry-jitter',
        type: 'convention',
        label: 'All retry policies must use full jitter, not fixed backoff',
        content: 'Every retry policy in Riverstone is required to use full jitter (a random delay between 0 and the exponential backoff ceiling), never a fixed or exponential-only delay. This is enforced in code review because a fixed backoff across thousands of workers causes synchronized retry storms against downstream connectors.',
        tags: ['convention', 'retry-policy', 'backoff'],
        terse: 'retry policy jitter rule',
        chatty: 'What is our house rule about how retry delays have to be randomized across workers?',
    },
    {
        id: 'fx-convention-metrics-naming',
        type: 'convention',
        label: 'Metric names follow riverstone.<component>.<verb>_<unit> pattern',
        content: 'All metrics exported by any Riverstone component must follow the naming pattern riverstone.<component>.<verb>_<unit>, e.g. riverstone.scheduler.dispatch_ms. Metrics that don\'t follow this pattern are rejected at the metrics-exporter\'s validation layer before they reach the aggregator.',
        tags: ['convention', 'metrics-exporter', 'naming'],
        terse: 'metric naming convention',
        chatty: 'Is there a required naming pattern for the metrics that components emit?',
    },
    {
        id: 'fx-convention-webhook-signing',
        type: 'convention',
        label: 'Outbound webhooks must be HMAC-SHA256 signed with a rotating secret',
        content: 'Every outbound webhook fired by the webhook-relay must carry an HMAC-SHA256 signature computed with the current rotating secret, plus the previous secret\'s signature during the 24h rotation overlap window, so a receiver mid-rotation never rejects a legitimate delivery.',
        tags: ['convention', 'webhook-relay', 'security'],
        terse: 'webhook signature requirement',
        chatty: 'What are we required to do when signing outbound webhook payloads before they go out?',
    },
    {
        id: 'fx-architecture-checkpoint-store',
        type: 'architecture',
        label: 'Checkpoint store is a separate append-only log, not the job table',
        content: 'The checkpoint store that records long-running job progress is a dedicated append-only log, physically separate from the primary job table. This isolation exists so a checkpoint write under heavy load never contends for locks with job-status writes, which was the root cause of the 2025 scheduler stall.',
        tags: ['architecture', 'checkpoint-store', 'isolation'],
        terse: 'checkpoint store design',
        chatty: 'How is the checkpoint store for tracking long job progress structured relative to the main job table?',
    },
    {
        id: 'fx-architecture-connector-bus',
        type: 'architecture',
        label: 'Connector bus decouples workers from downstream systems via an outbox',
        content: 'Workers never call a downstream connector directly. Instead they write to a local outbox table, and the connector-bus drains the outbox asynchronously per connector, applying per-connector rate limits. This means a slow or down connector never blocks a worker\'s next job.',
        tags: ['architecture', 'connector-bus', 'outbox-pattern'],
        terse: 'connector bus outbox design',
        chatty: 'How does the connector bus keep a slow downstream system from blocking the workers that feed it?',
    },
    {
        id: 'fx-architecture-scheduler-tiers',
        type: 'architecture',
        label: 'Scheduler runs three priority tiers with strict tier starvation guard',
        content: 'The Riverstone scheduler maintains three priority tiers (interactive, batch, background). A starvation guard reserves at least 5% of dispatch slots for the lowest tier even under sustained high-priority load, so background jobs are never starved indefinitely.',
        tags: ['architecture', 'scheduler', 'priority'],
        terse: 'scheduler priority tiers',
        chatty: 'How many priority tiers does the scheduler have, and what stops the lowest one from starving?',
    },
    {
        id: 'fx-architecture-rate-limiter-token-bucket',
        type: 'architecture',
        label: 'Rate limiter uses a distributed token bucket, refilled via lease',
        content: 'The rate-limiter implements a distributed token bucket where each shard holds a lease on a slice of the global token budget, refilled every 200ms. This avoids a single point of contention while keeping the aggregate rate within 2% of the configured ceiling.',
        tags: ['architecture', 'rate-limiter', 'token-bucket'],
        terse: 'rate limiter token bucket design',
        chatty: 'What algorithm does the rate limiter use to enforce a global request ceiling across shards?',
    },
    {
        id: 'fx-bug-checkpoint-torn-write',
        type: 'bug_pattern',
        label: 'Torn checkpoint writes under process kill -9 during flush',
        content: 'A worker killed with SIGKILL mid-flush can leave a torn (partially written) checkpoint record, which the recovery path was reading as valid and resuming from a corrupted offset. Fix: checkpoint records now write to a temp file and rename atomically, so a killed flush leaves only the prior valid checkpoint.',
        tags: ['bug_pattern', 'checkpoint-store', 'torn-write'],
        terse: 'torn checkpoint write bug',
        chatty: 'What went wrong with checkpoints getting corrupted when a worker process was killed mid-write?',
    },
    {
        id: 'fx-bug-lease-double-execution',
        type: 'bug_pattern',
        label: 'Lease renewal race caused duplicate job execution under GC pause',
        content: 'A worker experiencing a long GC pause could miss its lease renewal deadline, have the lease reassigned to a second worker, then resume and finish the job itself too — double executing it. Fix: lease renewal now carries a fencing token that the job-completion write must present, rejecting the stale worker\'s completion.',
        tags: ['bug_pattern', 'lease', 'concurrency'],
        terse: 'duplicate job execution bug',
        chatty: 'How did a job end up getting executed twice because of a lease renewal race condition?',
    },
    {
        id: 'fx-bug-rate-limiter-thundering-herd',
        type: 'bug_pattern',
        label: 'Token bucket refill created a thundering herd every 200ms tick',
        content: 'All shards refilling their token slice on the exact same 200ms tick caused every waiting worker to retry in the same instant, spiking connector CPU. Fix: refill tick is now jittered per-shard by up to 40ms.',
        tags: ['bug_pattern', 'rate-limiter', 'thundering-herd'],
        terse: 'rate limiter thundering herd bug',
        chatty: 'What caused a burst of simultaneous retries every time the rate limiter refilled its tokens?',
    },
    {
        id: 'fx-bug-config-loader-hot-reload-leak',
        type: 'bug_pattern',
        label: 'Config hot-reload leaked file watchers on every reload',
        content: 'Every hot-reload of a config file created a new inotify watcher without releasing the previous one, exhausting the process\'s watch-descriptor limit after roughly 8,000 reloads over a long-lived deployment. Fix: the watcher is now created once at startup and reused across reloads.',
        tags: ['bug_pattern', 'config-loader', 'resource-leak'],
        terse: 'config reload watcher leak',
        chatty: 'Why did the process eventually run out of file watchers after config got hot-reloaded many times?',
    },
    {
        id: 'fx-convention-idempotency-key',
        type: 'convention',
        label: 'Every job submission must carry a caller-supplied idempotency key',
        content: 'Job submission requires a caller-supplied idempotency key. Submitting the same key twice within 24 hours returns the original job\'s id instead of creating a duplicate. Callers that omit the key are rejected at the API boundary, not silently defaulted.',
        tags: ['convention', 'idempotency', 'api'],
        terse: 'job submission idempotency key rule',
        chatty: 'What do callers have to provide when submitting a job so duplicate submissions do not create duplicate jobs?',
    },
    {
        id: 'fx-decision-webhook-relay-language',
        type: 'decision',
        label: 'Webhook relay rewritten in Rust for predictable tail latency',
        content: 'The webhook-relay was rewritten from its original Node.js implementation to Rust specifically to eliminate GC-pause-driven p99 latency spikes, which were causing webhook delivery to blow past downstream timeout windows during traffic bursts.',
        tags: ['decision', 'webhook-relay', 'rust'],
        terse: 'why webhook relay is in rust',
        chatty: 'What was the reasoning behind rewriting the webhook relay service into Rust?',
    },
    {
        id: 'fx-architecture-metrics-exporter-batching',
        type: 'architecture',
        label: 'Metrics exporter batches points and flushes on a 10s or 2000-point cap',
        content: 'The metrics-exporter buffers emitted points in memory and flushes to the aggregator whichever comes first: a 10-second timer or a 2000-point buffer cap. This bounds both worst-case staleness and worst-case memory use per exporter instance.',
        tags: ['architecture', 'metrics-exporter', 'batching'],
        terse: 'metrics exporter flush policy',
        chatty: 'Under what conditions does the metrics exporter actually flush its buffered points?',
    },
    {
        id: 'fx-bug-tenant-router-hot-shard',
        type: 'bug_pattern',
        label: 'Hash skew created a hot shard for high-volume tenants',
        content: 'Consistent hashing without virtual nodes placed three of Riverstone\'s highest-volume tenants on the same physical shard by coincidence, overloading it while neighboring shards sat idle. Fix: the tenant router now uses 128 virtual nodes per physical shard to smooth the distribution.',
        tags: ['bug_pattern', 'tenant-router', 'hot-shard'],
        terse: 'tenant router hot shard bug',
        chatty: 'Why did one shard in the tenant router end up overloaded while the others were mostly idle?',
    },
    {
        id: 'fx-convention-schema-migration-additive',
        type: 'convention',
        label: 'Schema migrations must be additive-only in the same release',
        content: 'A single release may only add columns, tables, or indexes to the job-table schema — never drop or rename in the same release. Drops/renames require a separate follow-up release at least one deploy cycle later, so a rollback of the additive release never breaks the running code.',
        tags: ['convention', 'schema-migration', 'safety'],
        terse: 'schema migration safety rule',
        chatty: 'What is the rule about what kinds of schema changes are allowed to ship in a single release?',
    },
    {
        id: 'fx-architecture-worker-pool-autoscale',
        type: 'architecture',
        label: 'Worker pool autoscaling reacts to queue depth slope, not absolute depth',
        content: 'The worker pool autoscaler triggers on the rate of change of queue depth over a 90-second window rather than the absolute queue depth. This avoids overreacting to a normal daily traffic ramp while still reacting fast to a genuine backlog spike.',
        tags: ['architecture', 'worker-pool', 'autoscaling'],
        terse: 'worker pool autoscaling trigger',
        chatty: 'What signal drives the decision to add more workers to the pool — queue size or something else?',
    },
    {
        id: 'fx-bug-dlq-purge-race',
        type: 'bug_pattern',
        label: 'DLQ purge job raced a manual replay and deleted the replayed copy',
        content: 'The 14-day DLQ auto-purge job and a manually triggered replay could race: the replay re-enqueued a job while the purge, already mid-scan, deleted the original DLQ entry out from under it, leaving no record the replay had happened. Fix: purge now takes a row-level lock per entry before deleting.',
        tags: ['bug_pattern', 'dlq', 'race-condition'],
        terse: 'dlq purge replay race bug',
        chatty: 'What went wrong when a manual DLQ replay happened at the same time as the automatic purge job?',
    },
    {
        id: 'fx-decision-observability-vendor',
        type: 'decision',
        label: 'Self-hosted metrics stack chosen over a SaaS observability vendor',
        content: 'Riverstone runs a self-hosted Prometheus-compatible metrics stack rather than a SaaS observability vendor, primarily driven by per-tenant data residency requirements from two large customers that prohibit shipping their operational metadata off-premises.',
        tags: ['decision', 'observability', 'compliance'],
        terse: 'why self-hosted metrics stack',
        chatty: 'Why did we choose to self-host the metrics/observability stack instead of using a SaaS vendor?',
    },
    {
        id: 'fx-convention-postmortem-blameless',
        type: 'convention',
        label: 'All incident postmortems are blameless and published within 5 business days',
        content: 'Every incident above severity 3 gets a blameless postmortem, published to the whole engineering org within 5 business days of resolution. "Blameless" specifically means the document may name systems and decisions but never an individual as the cause.',
        tags: ['convention', 'postmortem', 'incident-process'],
        terse: 'postmortem process rule',
        chatty: 'What is our policy on writing up and sharing postmortems after an incident?',
    },
];
