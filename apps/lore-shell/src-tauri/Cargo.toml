[package]
name = "lore-shell"
version = "0.1.0"
description = "Tauri host shell for the Lore local app"
authors = ["Groundfloor"]
license = "MIT"
edition = "2021"
rust-version = "1.77"

[lib]
name = "lore_shell_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[features]
# This feature is used to produce builds with bundled platform features
# (the default). Disable for headless test builds.
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
